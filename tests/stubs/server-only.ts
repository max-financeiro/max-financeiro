// Stub vazio pra Vitest. Em runtime real (Next.js) o `import 'server-only'`
// resolve pro pacote oficial que crasha se importado de Client Component.
// Em test environment não temos Client vs Server, então nada-op.
export {};
